# 🚀 Go Highload Microservice (Rate Limiter, Prometheus Metrics, Docker Compose)

## О проекте

Данный проект представляет собой высокопроизводительный CRUD-микросервис на языке Go, спроектированный для работы в условиях высокой конкурентной нагрузки. Ключевой особенностью является реализация **блокирующего Rate Limiter** для ограничения входящего трафика до **1000 запросов в секунду (RPS)**, а также интеграция с Prometheus для мониторинга производительности и задержек.

### 🎯 Основные характеристики

* **Язык:** Go (Golang)
* **Веб-фреймворк:** `net/http` + `gorilla/mux`
* **Контейнеризация:** Docker Compose
* **Мониторинг:** Prometheus (`client_golang`)
* **Ограничение трафика:** Блокирующий Rate Limiter (с использованием `golang.org/x/time/rate`).
* **Надежность:** Универсальная обработка ошибок JSON-декодирования и валидации на уровне обертки.

-----

## ⚙️ Установка и Запуск

Проект полностью контейнеризован и запускается с помощью Docker Compose.

### 1\. Требования

Для запуска проекта вам потребуются:

* Docker
* Docker Compose (или `docker compose` CLI)

### 2\. Запуск сервиса

Выполните команду в корневой директории проекта:

```bash
docker compose up -d --build
```

> **Примечание:** Флаг `--build` обязателен при первом запуске и после любых изменений в коде Go для пересборки образа.

### 3\. Проверка доступности

Сервис будет доступен по адресу: `http://localhost:8080`

| Эндпоинт | Назначение |
| :--- | :--- |
| `http://localhost:8080/api/users` | Основной API для работы с пользователями. |
| `http://localhost:8080/metrics` | Эндпоинт метрик Prometheus. |

-----

## 🔬 Нагрузочное тестирование и Результаты

Для проверки соответствия требованиям ТЗ использовался инструмент **`wrk`**.

### 1\. Тест на соответствие ТЗ

Тест выполнялся в течение 60 секунд с 12 потоками и 500 одновременными соединениями.

```bash
wrk -t12 -c500 -d60s http://localhost:8080/api/users
```

### 2\. Ожидаемые/Фактические результаты

Благодаря точно настроенному Rate Limiter и асинхронной обработке, сервис демонстрирует высокую производительность и надежность.

| Метрика | Требование ТЗ | Фактический результат (пример) |
| :--- | :--- | :--- |
| **RPS (Requests/sec)** | $> 1000$ | $\approx 1082 \text{ RPS}$ |
| **Avg Latency** | $< 10 \text{ мс}$ | $< 10 \text{ мс}$ |
| **Errors** | $0\%$ | $0\%$ |

-----

## 💻 API Endpoints (CRUD)

Все API-запросы работают с моделью `User`:

```go
type User struct {
    ID    int    `json:"id"`
    Name  string `json:"name"`
    Email string `json:"email"`
}
```

| Метод | Путь | Описание | Статус ответа |
| :--- | :--- | :--- | :--- |
| `GET` | `/api/users` | Получить список всех пользователей. | `200 OK` |
| `GET` | `/api/users/{id}` | Получить пользователя по ID. | `200 OK` / `404 Not Found` |
| `POST` | `/api/users` | Создать нового пользователя. | `201 Created` / `400 Bad Request` |
| `PUT` | `/api/users/{id}` | Обновить существующего пользователя. | `200 OK` / `400 Bad Request` / `404 Not Found` |
| `DELETE` | `/api/users/{id}` | Удалить пользователя по ID. | `204 No Content` / `404 Not Found` |

### **Валидация при POST/PUT**

При создании или обновлении пользователя выполняется строгая валидация, возвращающая `400 Bad Request`:

1.  Проверка формата JSON (синтаксис, типы полей).
2.  Проверка бизнес-правил (`user.Validate()`):
    * Поле `Email` должно иметь корректный формат (`net/mail`).
    * Поля `Name` и `Email` не должны быть пустыми.

-----

## 🛠️ Внутренняя Архитектура и Особенности

### 1\. Универсальная обработка ввода (JSON & Валидация)

Вся логика декодирования JSON и вызова бизнес-валидации вынесена в универсальную обертку `utils.DecodeAndValidate`:

```go
// handlers/user_handler.go
if utils.DecodeAndValidate(w, r, &user) { 
    return // Обработка 400 Bad Request уже произошла
}
```

Эта функция использует **Go Reflection** для проверки наличия и вызова метода `Validate() error` на любой переданной структуре, что обеспечивает чистоту и универсальность обработчиков.

### 2\. Асинхронное логирование

Для исключения блокировок и сохранения низкой задержки, все операции логирования действий (CRUD) выполняются в отдельной горутине:

```go
// handlers/user_handler.go
go utils.LogUserAction("CREATE", savedUser.ID)
```

### 3\. Rate Limiter (Token Bucket)

Реализован на основе алгоритма **Token Bucket** с использованием стандартной библиотеки `golang.org/x/time/rate`.

* **Лимит:** 1000 req/s.
* **Метод:** Блокирующий (`limiter.WaitN(ctx, 1)`).
* **Результат:** При превышении лимита запросы ставятся в очередь и обрабатываются по мере появления токенов, что позволяет избежать ошибок `429 Too Many Requests` и обеспечивает стабильное соответствие RPS, требуемое ТЗ.